name: Release

on:
  release:
    types:
      - created

env:
  CARGO_TERM_COLOR: always
  BIN_NAME: ariarouter

jobs:
  build:
    name: Build & Test (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            asset_os: linux_x86_64
            archive: tar.gz
          - name: windows-x86_64
            os: windows-latest
            asset_os: windows_x86_64
            archive: zip
          - name: linux-arm64
            os: ubuntu-24.04-arm
            asset_os: linux_arm64
            archive: tar.gz
          - name: macos
            os: macos-latest
            asset_os: macos
            archive: tar.gz
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Build
        run: cargo build --workspace

      - name: Test
        run: cargo test --workspace

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build release binary
        shell: bash
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail
          # Embed release tag (sans leading v) into ariarouter -v/--version.
          export ARIAROUTER_VERSION="${TAG_NAME#v}"
          echo "ARIAROUTER_VERSION=$ARIAROUTER_VERSION"
          cargo build --release -p ariarouter
          cargo build --release -p ariacompute-ariarouter-ffi

      - name: Package release archive (Unix)
        if: matrix.archive == 'tar.gz'
        shell: bash
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          ASSET_OS: ${{ matrix.asset_os }}
        run: |
          set -euo pipefail
          VERSION="${TAG_NAME#v}"
          ARCHIVE_NAME="ariarouter_${VERSION}_${ASSET_OS}.tar.gz"
          tar -czf "$ARCHIVE_NAME" -C target/release "$BIN_NAME"
          echo "ASSET_PATH=$ARCHIVE_NAME" >> "$GITHUB_ENV"

      - name: Package release archive (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          ASSET_OS: ${{ matrix.asset_os }}
        run: |
          $version = $env:TAG_NAME.TrimStart('v')
          $archiveName = "ariarouter_${version}_$($env:ASSET_OS).zip"
          Compress-Archive -Path "target/release/$($env:BIN_NAME).exe" -DestinationPath $archiveName -Force
          Add-Content -Path $env:GITHUB_ENV -Value "ASSET_PATH=$archiveName"

      - name: Package libariarouter_ffi (Unix)
        if: matrix.archive == 'tar.gz'
        shell: bash
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          ASSET_OS: ${{ matrix.asset_os }}
        run: |
          set -euo pipefail
          VERSION="${TAG_NAME#v}"
          ARCHIVE_NAME="libariarouter_ffi_${VERSION}_${ASSET_OS}.tar.gz"
          cd target/release
          if [[ -f libariarouter_ffi.dylib ]]; then
            tar -czf "../../$ARCHIVE_NAME" libariarouter_ffi.dylib
          elif [[ -f libariarouter_ffi.so ]]; then
            tar -czf "../../$ARCHIVE_NAME" libariarouter_ffi.so
          elif [[ -f libariarouter_ffi.a ]]; then
            tar -czf "../../$ARCHIVE_NAME" libariarouter_ffi.a
          else
            echo "No libariarouter_ffi shared/static library found in target/release" >&2
            ls -la
            exit 1
          fi
          cd ../..
          echo "FFI_ASSET_PATH=$ARCHIVE_NAME" >> "$GITHUB_ENV"

      - name: Package libariarouter_ffi (Windows)
        if: matrix.archive == 'zip'
        shell: bash
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          ASSET_OS: ${{ matrix.asset_os }}
        run: |
          set -euo pipefail
          VERSION="${TAG_NAME#v}"
          ARCHIVE_NAME="libariarouter_ffi_${VERSION}_${ASSET_OS}.tar.gz"
          cd target/release
          if [[ -f ariarouter_ffi.dll ]]; then
            FILES=(ariarouter_ffi.dll)
            [[ -f ariarouter_ffi.dll.lib ]] && FILES+=(ariarouter_ffi.dll.lib)
            [[ -f ariarouter_ffi.lib ]] && FILES+=(ariarouter_ffi.lib)
            tar -czf "../../$ARCHIVE_NAME" "${FILES[@]}"
          else
            echo "No ariarouter_ffi.dll found in target/release" >&2
            ls -la
            exit 1
          fi
          cd ../..
          echo "FFI_ASSET_PATH=$ARCHIVE_NAME" >> "$GITHUB_ENV"

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: |
            ${{ env.ASSET_PATH }}
            ${{ env.FFI_ASSET_PATH }}
          token: ${{ secrets.ARIACOMPUTE_TOKEN }}

  # Language package publishes are fail-pass: must not block CLI/assets.
  publish-packages:
    name: Publish language packages (fail-pass)
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Set package version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "VERSION=$VERSION"
      - name: npm @ariacompute/ariarouter-ts + router-rn
        continue-on-error: true
        run: |
          echo "npm publish handled by publish-npm.yml (router-ts / router-rn)"
          echo "npm publish stub (fail-pass)"
      - name: pub.dev package:aria_router
        continue-on-error: true
        run: |
          echo "pub.dev publish handled by publish-pub.yml (publisher ariacompute.com, package:aria_router)"
          echo "pub.dev publish stub (fail-pass)"
      - name: Maven Central com.ariacompute:router
        continue-on-error: true
        run: |
          echo "Maven publish handled by publish-maven.yml (vanniktech plugin -> Central Portal)"
          echo "Maven publish stub (fail-pass)"
      - name: CocoaPods Swift
        continue-on-error: true
        run: |
          echo "Configure COCOAPODS_TRUNK_TOKEN then: pod trunk push bindings/swift/Ariarouter.podspec"
          echo "CocoaPods publish stub (fail-pass)"
      - name: crates.io ariacompute-ariarouter
        continue-on-error: true
        run: |
          echo "crates.io publish handled by publish-cargo.yml (ariacompute-ariarouter + deps)"
          echo "crates.io publish stub (fail-pass)"
      - name: PyPI ariarouter
        continue-on-error: true
        run: |
          echo "PyPI publish handled by publish-pypi.yml (cibuildwheel platform wheels)"
          echo "PyPI publish stub (fail-pass)"
