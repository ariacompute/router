[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ariacompute-router"
version = "0.1.0"
description = "Python SDK for aria-router (libaria_router_ffi)"
requires-python = ">=3.10"
