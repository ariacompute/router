# aria_router

Flutter SDK for [aria-router](https://github.com/ariacompute/router). Instance `auth` is in-memory (`baseUrl` / `token`) and does not write `config.yml`. Native FFI uses `libaria_router_ffi`.
