# aria_router

Flutter SDK for [aria-router](https://github.com/ariacompute/router). Instance `setup` is in-memory (`baseUrl` / `token`) and does not write `router.yml`. Native FFI uses `libaria-router_ffi`.
